# Dependencies
node_modules/

# Build caches / output
*.tsbuildinfo

# Env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Preview pipeline artifacts (fork-derived, window may still be open — never root-safe)
offchain/preview-deposits.json
offchain/build-config.json
offchain/merkle-preview.json
